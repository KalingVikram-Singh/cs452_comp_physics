To view the output, kindly look at outputfiles.ipynb. It is a jupyter notebook, so it can be easily viewed when open in github itself.
To run, donwload all files in a folder and run outfiles.ipynb in jupyter notebook to get the results
